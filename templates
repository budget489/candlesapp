templates/index.html
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Interpretador de Velas</title>
</head>
<body style="font-family: sans-serif; max-width: 700px; margin: auto;">
    <h1>📊 Análisis de Velas Japonesas</h1>
    <form method="post">
        <input type="text" name="ticker" placeholder="Ej: AAPL, TSLA, JD" required>
        <button type="submit">Analizar</button>
    </form>
    <hr>
    {% for r in resultados %}
        <p>{{ r }}</p>
    {% endfor %}
</body>
</html>
